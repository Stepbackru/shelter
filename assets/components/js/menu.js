import * as C from '../js/constants.js';

const menu = () => {
  document.querySelector(`.${C.MENU}`).addEventListener('click', (e) => {
    const item = e.target;
    

  });
}

export default menu;
